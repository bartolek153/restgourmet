services:
  cache:
    image: redis:7.4.2
    ports:
      - "6379:6379"

  db:
    image: postgres:14.2
    ports:
      - "5432:5432"

    # set shared memory limit when using docker-compose
    shm_size: 128mb

    environment:
      POSTGRES_PASSWORD: password
