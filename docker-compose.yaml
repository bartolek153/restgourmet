services:

  db:
    image: postgres:14.2
    restart: always
    ports:
      - "5432:5432"

    # set shared memory limit when using docker-compose
    shm_size: 128mb

    environment:
      POSTGRES_PASSWORD: password
